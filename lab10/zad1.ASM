clo
mov dl,c0 ; указываем реестру дл €чейку с0 ( он же адрес с0 )
jmp rep ; ( прыжок к циклу rep )

check: ; сравниваем дошел ли реестр дл до адреса cf
cmp dl,cf

jz restart ; если заполнено то прыжок в рестарт
jnz rep ; если не заполнено то прыжок к реп

restart:
mov dl,c0 ; задаем заново реестру дл €чейку с0, чтобы экономить пам€ть
jmp rep ; прыжок к реп

rep:
in 00 ; считываем знак с клавиатуры
cmp al,0d ; сравниваем был ли этот знак enter 
pushf ; сохран€ем флаг перед подсчетными операци€ми, потому что inc dl изменит этот флаг 
mov [dl],al ; сохран€ем знак аль в реестр дл в котором адрес с0, чтобы сразу вывести этот знак
inc dl ; переходим к следующей €чейке реестра дл
popf ; достаем флаг после операций 
jnz check ; если это не был знак enter то прыжок к check
end

