jmp start ; необходим нажать на букву K ровно сверху над словом "ровно" в этой строке
db 70 ; указатель на €чейку орг 70
db 50 
start:
STI ; включаем часы  
cmp al, 0D ; если нажать знак enter то программа восстановит змейку к началу
jz reset ; прыжок в ресет
cmp al,77 ; если нажал w то змейка ползет вверх
jz up
cmp al,73 ; если нажал s то змейка ползет вниз
jz down
cmp al,61 ; если нажал a то змейка ползет влево
jz left
cmp al, 64 ; если нажал d то змейка ползет вправо
jz right
jmp start ; если не нажал то провер€ет заново какой знак ты успел нажать

reset:
mov al, FF ; FF восстанавливает полностью змейку
out 04 ; вывод знака в змейку
jmp start ; прыжок в проверку нажатого знака

up:
mov al,8F ; аль принимает значение дл€ того , чтобы змейка ползла вверх
out 04; вывод знака в змейку
mov al,0
jmp start

down:
mov al,4F; аль принимает значение дл€ того , чтобы змейка ползла вниз
out 04; вывод знака в змейку
mov al,0
jmp start

left:
mov al,2F; аль принимает значение дл€ того , чтобы змейка ползла влево
out 04; вывод знака в змейку
mov al,0
jmp start

right:
mov al,1F; аль принимает значение дл€ того , чтобы змейка ползла вправо
out 04; вывод знака в змейку
mov al,0 
jmp start

org 70
in 07 ; самое основное в том, что выведенна€ клавиатура считывает введенный знак раз в 1 сек если ты установил 1 сек
IRET ; возвращает код в самое начало

org 50 
iret ; возвращает код в самое начало

end
